import 'package:flutter/material.dart';

class BookEntryLoading extends StatelessWidget {
  const BookEntryLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
